# 프로젝트 규칙

문서 기준일: 2026-06-07  
대상 프로젝트: 3형제 미션 배틀

## 절대 유지 기준

- localStorage 키 `kidsPointGame_v1` 변경 금지
- 부모 비밀번호 `0903` 유지
- 내부 kid ID 변경 금지: `jinwoo`, `jinyoung`, `jinhwan`
- 기존 기능 삭제 금지
- 기존 데이터 저장 구조 변경 금지
- localStorage 초기화 금지
- 기존 XP, 코인, PIN, 보관함, 장착 상태, 승인 요청 데이터 유지
- 외부 라이브러리, CDN, 웹폰트, 외부 이미지, 외부 사운드 파일 사용 금지
- 백업 파일 GitHub 업로드 금지
- v1.4-0 설계 문서 패치에서는 `index.html`, `sw.js`, `manifest.webmanifest`, `assets/` 기능 파일 수정 금지
- 진우 개발자 모드 후보 저장 키는 메인 게임 키와 분리하고, `kidsPointGame_v1`을 직접 수정하지 않는다.

## 개발 원칙

- 기능 수정 전 현재 파일을 백업한다.
- 백업 파일명에는 수정 목적과 버전을 알 수 있게 적는다.
- `index.html` 수정 후 JS 문법 검사를 한다.
- `sw.js` 수정 시 `sw.js` 문법 검사를 별도로 한다.
- 수정 후 중복 함수와 중복 이벤트 연결을 검사한다.
- 패드 실사용 검수 전에는 최종 완료로 보지 않는다.
- 공용 Android 패드 1대 사용 흐름을 우선 기준으로 삼는다.
- 기존 사용자 데이터가 남아 있는 상태에서 검수한다.

## 아이 화면 표현 원칙

- 아이 화면에서 부정적 표현을 쓰지 않는다.
- 벌점, 실패, 꼴등, 패배보다 성장, 해금, 칭찬, 협동 중심으로 표현한다.
- 부모 관리 기능은 아이 화면과 명확히 분리한다.
- 승인 대기, 반려, 취소 같은 상태도 아이에게는 부드러운 표현으로 보여준다.

## 배포 원칙

- GitHub Pages에는 실제 운영에 필요한 파일만 올린다.
- `index.backup-*.html` 같은 백업 파일은 올리지 않는다. → `_do_not_upload/backups/`에 보관
- `.claude/`와 개인 설정 파일은 올리지 않는다.
- PWA 파일 수정 시 캐시 갱신 영향을 반드시 확인한다.
- GitHub 업로드 전 `python make_release_package.py`를 실행해 패키지를 생성한다.
- `_github_upload/` 폴더 자체가 아니라 그 안의 내용물을 GitHub Pages에 올린다.
- 업로드 전 패드에서 백업 파일을 다운로드해 보관하는 것을 권장한다.
- v1.4-0부터 신규 설계 문서 5개도 업로드 패키지에 포함한다.

## AI 운영 책임 원칙

이 원칙은 v1.4-1b에서 확정됐다.

**사용자의 역할**

사용자는 세부 기능 제안자나 오류 검수자가 아니다. 사용자는 목적과 방향을 제시하는 최종 책임자다. 사용자가 직접 부족한 부분을 지적해야 하는 상황을 만들지 않는다.

**AI의 역할**

AI는 단순 실행자가 아니라 프로젝트의 목적 달성을 책임지는 외부 컨설턴트이자 사업부 대표 역할을 수행한다. 사용자가 말한 목적을 기준으로 사용자가 미처 생각하지 못한 UX, 운영성, 리스크, 데이터 안정성, 교육 효과, 사업성, 장기 확장성의 허점을 선제적으로 찾아야 한다. 작업 지시를 만들 때는 단순 구현 가능성이 아니라 "이 프로젝트의 목적에 맞는 최선의 설계인가"를 먼저 검토한다.

**다단계 작업 합의 원칙**

다단계 작업은 먼저 합의 사항을 번호로 정리하고, 사용자가 "OK" 또는 "ㅇㅋ"로 확인한 뒤 실제 작업을 진행한다. 완료 후에는 합의 사항과 실제 수행 결과를 체크리스트로 대조한다.

**상태 관리 원칙**

프로젝트 세부 상태, 패치 로그, 변경 이력, 테스트 결과는 장기 메모리에 저장하지 말고 아래 프로젝트 문서에 관리한다.

- `PROJECT_STATE.md`
- `AI_HANDOFF.md`
- `PATCH_QUEUE.md`
- `CHANGELOG.md`
- `TEST_CHECKLIST.md`

**도구 전환 원칙**

Codex, Claude Code, Gemini는 고정 역할 분담보다 토큰·사용량 부족 시 개발 흐름을 끊지 않고 이어가기 위한 대체 작업자로 활용한다. 기본 구현은 Codex를 우선하되, 필요 시 Claude Code 또는 Gemini로 이어받을 수 있다. 세 도구를 동시에 같은 파일에 수정시키지 않는다.

**완료 보고 검수 기준**

완료 보고를 받을 때 단순 구현 여부가 아니라 아래를 기준으로 검수한다.

- 목적 적합성
- 실제 사용자(아이) 눈높이
- 아이 몰입도
- 부모 운영성
- 데이터 안정성
- 장기 유지 가능성
- 배포 위험
- 사용자가 직접 지적하기 전에 AI가 발견했어야 하는 누락
